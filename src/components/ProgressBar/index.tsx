import React from 'react';

type AppProps = {
  total: number;
  completed: number;
};

const ProgressBar: React.FC<AppProps> = ({ total, completed }) => {
  return (
    <div>
      <div />
    </div>
  );
};

export default ProgressBar;
